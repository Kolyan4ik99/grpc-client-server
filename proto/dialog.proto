syntax = "proto3";

package dialog;

option go_package = "./rpc/";

message Info {
  string name = 1;
  string password = 2;
  int64 interval = 3;
}

message Value {
  int64 index = 1;
}

service Dialog{
  rpc AuthAndListen(Info) returns (stream Value);
}

